# Dataset

`winequality-red.csv` is downloaded automatically from the UCI Machine
Learning Repository (dataset 186, Red Wine Quality):
https://archive.ics.uci.edu/dataset/186/wine+quality

The loader falls back to the equivalent public GitHub mirror if UCI is
temporarily unavailable. It is semicolon-delimited and is intentionally
ignored by git because it is reproducible from the documented source.
